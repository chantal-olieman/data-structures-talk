from src.binary_tree import BinaryTreeNode


def get_values_by_level(root: BinaryTreeNode):
    """
    A function that returns a list containing the value of all nodes in the tree, 
    ordered by the level of debt calculated from the root 
    :param root: the root of the tree of type BinaryTreeNode
    :return: list containing the value of all nodes in the tree.
    """